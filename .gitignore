*.nupkg
*.dll
*.pdb
*.dylib
*.so
*.zip

# deps working dirs (vcpkg build output + fetched prebuilt archives).
# Only the manifest is tracked; everything else under deps/ is generated.
/deps/*
!/deps/vcpkg.json
